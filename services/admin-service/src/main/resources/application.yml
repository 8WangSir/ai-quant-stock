server:
  port: 8080

spring:
  application:
    name: admin-service
  datasource:
    url: jdbc:postgresql://localhost:15432/quant_stock?sslmode=disable
    username: quant
    password: quant123
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

logging:
  file:
    name: c:/Users/wangw/ai-quant-stock/services/admin-service/logs/admin-service.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
